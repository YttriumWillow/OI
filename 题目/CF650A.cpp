#include <bits/stdc++.h>
#define qwq puts("fzy qwq ~");
#define longint long long
#define reg register
#define longpair longint, longint
#define _read =read()
using namespace std;
longint n, m, ans, x, y;
map<longpair> mapx;
map<longpair> mapy;
int main()
{
    scanf("%lld", &n);
    for (int i = 1; i <= n; i++)
    {
        scanf("%lld%lld", &x, &y);
        mapx[y];
    }
    return 0;
}