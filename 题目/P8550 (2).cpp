#include <bits/stdc++.h>
#define longint long long
#define reg register
#define 泣かないでわたしの恋心 cin >> n >> x; flag = true;
#define 涙はお前にゃ似合わない for (reg int i = 1; i <= n; i++)
#define ゆけただゆけ { cin >> a; if ( !(x % a == 0 && a * x <= 0) ) flag = false; }
#define いっそわたしがゆくよ if (flag) cout << "No" << endl;
#define ああ心が笑いたがっている else cout << "Yes" << endl;
using namespace std;
int n, x, T, a;
bool flag;
int main()
{
    cin >> T;
    while (T--)
    {
        泣かないでわたしの恋心
    //  不要哭泣啊 我的爱恋之心
        涙はお前にゃ似合わない
    //  眼泪和 "你" 并不相配
        ゆけただゆけ
    //  走吧 只是向前
        いっそわたしがゆくよ
    //  我已下定决心就这样大步向前
        ああ心が笑いたがっている
    //  啊啊 虽然内心没有了笑容
    }
    return 0; // 完全勝利！
}
