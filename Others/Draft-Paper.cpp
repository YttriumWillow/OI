#include <iostream>
#include <set>

#define i64 long long
#define rep(i, l, r) for (int i = (l); i <= r; ++i)

int main(/*int argc, char const* argv[]*/) {
    rep (T, 1, 10) {
        int v = 0;
        rep (i, 1, 2 * (rand() % 5) + 1) {
            v = 1 << (rand() % 10)
        } 
    }
}