#include <bits/stdc++.h>
#define qwq puts("fzy qwq ~");
#define longint long long
#define reg register
#define _read =read();
using namespace std;
longint n, m, ans;
char ch;
int main()
{
    while (scanf("%c", &ch) != EOF)
        if (ch == '1')
            ans++;
    cout << ans << endl;
    return 0;
}