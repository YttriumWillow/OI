#include <iostream>

#define i64 long long 
#define endl '\n'
#define qwq puts("fzy qwq ~");

using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);

	cin >> n >> m >> k;

	rep (i, 1, n) cin >> r[i];
	rep (i, 1, n) cin >> d[i];

	rep (i, 1, n) rep (j, 1, n) f[i][r[i]] = f[r[i]][i] = 
	return 0;
}